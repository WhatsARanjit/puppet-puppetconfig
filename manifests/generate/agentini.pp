class puppet::generate::agentini (
  $req = {
    reports                              => $puppet::params::agent::reports,
    classfile                            => $puppet::params::agent::classfile,
    localconfig                          => $puppet::params::agent::localconfig,
    graph                                => $puppet::params::agent::graph,
    pluginsync                           => $puppet::params::agent::pluginsync,
    environment                          => $puppet::params::agent::environment,
  },
  $ini = {
    confdir                              => $puppet::params::agent::confdir,
    vardir                               => $puppet::params::agent::vardir,
    section                              => $puppet::params::agent::section,
    logdir                               => $puppet::params::agent::logdir,
    priority                             => $puppet::params::agent::priority,
    trace                                => $puppet::params::agent::trace,
    profile                              => $puppet::params::agent::profile,
    autoflush                            => $puppet::params::agent::autoflush,
    syslogfacility                       => $puppet::params::agent::syslogfacility,
    statedir                             => $puppet::params::agent::statedir,
    rundir                               => $puppet::params::agent::rundir,
    genmanifest                          => $puppet::params::agent::genmanifest,
    configprint                          => $puppet::params::agent::configprint,
    color                                => $puppet::params::agent::color,
    mkusers                              => $puppet::params::agent::mkusers,
    manage_internal_file_permissions     => $puppet::params::agent::manage_internal_file_permissions,
    onetime                              => $puppet::params::agent::onetime,
    path                                 => $puppet::params::agent::path,
    libdir                               => $puppet::params::agent::libdir,
    ignoreimport                         => $puppet::params::agent::ignoreimport,
    diff_args                            => $puppet::params::agent::diff_args,
    diff                                 => $puppet::params::agent::diff,
    show_diff                            => $puppet::params::agent::show_diff,
    daemonize                            => $puppet::params::agent::daemonize,
    maximum_uid                          => $puppet::params::agent::maximum_uid,
    route_file                           => $puppet::params::agent::route_file,
    node_terminus                        => $puppet::params::agent::node_terminus,
    node_cache_terminus                  => $puppet::params::agent::node_cache_terminus,
    data_binding_terminus                => $puppet::params::agent::data_binding_terminus,
    hiera_config                         => $puppet::params::agent::hiera_config,
    binder                               => $puppet::params::agent::binder,
    binder_config                        => $puppet::params::agent::binder_config,
    catalog_terminus                     => $puppet::params::agent::catalog_terminus,
    catalog_cache_terminus               => $puppet::params::agent::catalog_cache_terminus,
    facts_terminus                       => $puppet::params::agent::facts_terminus,
    inventory_terminus                   => $puppet::params::agent::inventory_terminus,
    default_file_terminus                => $puppet::params::agent::default_file_terminus,
    httplog                              => $puppet::params::agent::httplog,
    http_proxy_host                      => $puppet::params::agent::http_proxy_host,
    http_proxy_port                      => $puppet::params::agent::http_proxy_port,
    filetimeout                          => $puppet::params::agent::filetimeout,
    queue_type                           => $puppet::params::agent::queue_type,
    queue_source                         => $puppet::params::agent::queue_source,
    async_storeconfigs                   => $puppet::params::agent::async_storeconfigs,
    thin_storeconfigs                    => $puppet::params::agent::thin_storeconfigs,
    config_version                       => $puppet::params::agent::config_version,
    zlib                                 => $puppet::params::agent::zlib,
    prerun_command                       => $puppet::params::agent::prerun_command,
    postrun_command                      => $puppet::params::agent::postrun_command,
    freeze_main                          => $puppet::params::agent::freeze_main,
    stringify_facts                      => $puppet::params::agent::stringify_facts,
    trusted_node_data                    => $puppet::params::agent::trusted_node_data,
    certname                             => $puppet::params::agent::certname,
    certdnsnames                         => $puppet::params::agent::certdnsnames,
    dns_alt_names                        => $puppet::params::agent::dns_alt_names,
    csr_attributes                       => $puppet::params::agent::csr_attributes,
    certdir                              => $puppet::params::agent::certdir,
    ssldir                               => $puppet::params::agent::ssldir,
    publickeydir                         => $puppet::params::agent::publickeydir,
    requestdir                           => $puppet::params::agent::requestdir,
    privatekeydir                        => $puppet::params::agent::privatekeydir,
    privatedir                           => $puppet::params::agent::privatedir,
    passfile                             => $puppet::params::agent::passfile,
    hostcsr                              => $puppet::params::agent::hostcsr,
    hostcert                             => $puppet::params::agent::hostcert,
    hostprivkey                          => $puppet::params::agent::hostprivkey,
    hostpubkey                           => $puppet::params::agent::hostpubkey,
    localcacert                          => $puppet::params::agent::localcacert,
    ssl_client_ca_auth                   => $puppet::params::agent::ssl_client_ca_auth,
    ssl_server_ca_auth                   => $puppet::params::agent::ssl_server_ca_auth,
    hostcrl                              => $puppet::params::agent::hostcrl,
    certificate_revocation               => $puppet::params::agent::certificate_revocation,
    certificate_expire_warning           => $puppet::params::agent::certificate_expire_warning,
    plugindest                           => $puppet::params::agent::plugindest,
    pluginsource                         => $puppet::params::agent::pluginsource,
    pluginfactdest                       => $puppet::params::agent::pluginfactdest,
    pluginfactsource                     => $puppet::params::agent::pluginfactsource,
    pluginsignore                        => $puppet::params::agent::pluginsignore,
    factpath                             => $puppet::params::agent::factpath,
    external_nodes                       => $puppet::params::agent::external_nodes,
    module_repository                    => $puppet::params::agent::module_repository,
    module_working_dir                   => $puppet::params::agent::module_working_dir,
    module_skeleton_dir                  => $puppet::params::agent::module_skeleton_dir,
    ca_name                              => $puppet::params::agent::ca_name,
    cadir                                => $puppet::params::agent::cadir,
    cacert                               => $puppet::params::agent::cacert,
    cakey                                => $puppet::params::agent::cakey,
    capub                                => $puppet::params::agent::capub,
    cacrl                                => $puppet::params::agent::cacrl,
    caprivatedir                         => $puppet::params::agent::caprivatedir,
    csrdir                               => $puppet::params::agent::csrdir,
    signeddir                            => $puppet::params::agent::signeddir,
    capass                               => $puppet::params::agent::capass,
    serial                               => $puppet::params::agent::serial,
    autosign                             => $puppet::params::agent::autosign,
    allow_duplicate_certs                => $puppet::params::agent::allow_duplicate_certs,
    ca_ttl                               => $puppet::params::agent::ca_ttl,
    req_bits                             => $puppet::params::agent::req_bits,
    keylength                            => $puppet::params::agent::keylength,
    cert_inventory                       => $puppet::params::agent::cert_inventory,
    config_file_name                     => $puppet::params::agent::config_file_name,
    config                               => $puppet::params::agent::config,
    pidfile                              => $puppet::params::agent::pidfile,
    bindaddress                          => $puppet::params::agent::bindaddress,
    user                                 => $puppet::params::agent::user,
    group                                => $puppet::params::agent::group,
    manifestdir                          => $puppet::params::agent::manifestdir,
    manifest                             => $puppet::params::agent::manifest,
    code                                 => $puppet::params::agent::code,
    masterlog                            => $puppet::params::agent::masterlog,
    masterhttplog                        => $puppet::params::agent::masterhttplog,
    masterport                           => $puppet::params::agent::masterport,
    node_name                            => $puppet::params::agent::node_name,
    bucketdir                            => $puppet::params::agent::bucketdir,
    rest_authconfig                      => $puppet::params::agent::rest_authconfig,
    ca                                   => $puppet::params::agent::ca,
    modulepath                           => $puppet::params::agent::modulepath,
    ssl_client_header                    => $puppet::params::agent::ssl_client_header,
    ssl_client_verify_header             => $puppet::params::agent::ssl_client_verify_header,
    yamldir                              => $puppet::params::agent::yamldir,
    server_datadir                       => $puppet::params::agent::server_datadir,
    reportdir                            => $puppet::params::agent::reportdir,
    reporturl                            => $puppet::params::agent::reporturl,
    fileserverconfig                     => $puppet::params::agent::fileserverconfig,
    strict_hostname_checking             => $puppet::params::agent::strict_hostname_checking,
    storeconfigs                         => $puppet::params::agent::storeconfigs,
    storeconfigs_backend                 => $puppet::params::agent::storeconfigs_backend,
    rrddir                               => $puppet::params::agent::rrddir,
    rrdinterval                          => $puppet::params::agent::rrdinterval,
    devicedir                            => $puppet::params::agent::devicedir,
    deviceconfig                         => $puppet::params::agent::deviceconfig,
    node_name_value                      => $puppet::params::agent::node_name_value,
    node_name_fact                       => $puppet::params::agent::node_name_fact,
    statefile                            => $puppet::params::agent::statefile,
    clientyamldir                        => $puppet::params::agent::clientyamldir,
    client_datadir                       => $puppet::params::agent::client_datadir,
    resourcefile                         => $puppet::params::agent::resourcefile,
    puppetdlog                           => $puppet::params::agent::puppetdlog,
    server                               => $puppet::params::agent::server,
    use_srv_records                      => $puppet::params::agent::use_srv_records,
    srv_domain                           => $puppet::params::agent::srv_domain,
    ignoreschedules                      => $puppet::params::agent::ignoreschedules,
    default_schedules                    => $puppet::params::agent::default_schedules,
    puppetport                           => $puppet::params::agent::puppetport,
    runinterval                          => $puppet::params::agent::runinterval,
    listen                               => $puppet::params::agent::listen,
    ca_server                            => $puppet::params::agent::ca_server,
    ca_port                              => $puppet::params::agent::ca_port,
    catalog_format                       => $puppet::params::agent::catalog_format,
    preferred_serialization_format       => $puppet::params::agent::preferred_serialization_format,
    report_serialization_format          => $puppet::params::agent::report_serialization_format,
    legacy_query_parameter_serialization => $puppet::params::agent::legacy_query_parameter_serialization,
    agent_catalog_run_lockfile           => $puppet::params::agent::agent_catalog_run_lockfile,
    agent_disabled_lockfile              => $puppet::params::agent::agent_disabled_lockfile,
    usecacheonfailure                    => $puppet::params::agent::usecacheonfailure,
    use_cached_catalog                   => $puppet::params::agent::use_cached_catalog,
    ignoremissingtypes                   => $puppet::params::agent::ignoremissingtypes,
    ignorecache                          => $puppet::params::agent::ignorecache,
    dynamicfacts                         => $puppet::params::agent::dynamicfacts,
    splaylimit                           => $puppet::params::agent::splaylimit,
    splay                                => $puppet::params::agent::splay,
    clientbucketdir                      => $puppet::params::agent::clientbucketdir,
    configtimeout                        => $puppet::params::agent::configtimeout,
    report_server                        => $puppet::params::agent::report_server,
    report_port                          => $puppet::params::agent::report_port,
    inventory_server                     => $puppet::params::agent::inventory_server,
    inventory_port                       => $puppet::params::agent::inventory_port,
    report                               => $puppet::params::agent::report,
    lastrunfile                          => $puppet::params::agent::lastrunfile,
    lastrunreport                        => $puppet::params::agent::lastrunreport,
    graphdir                             => $puppet::params::agent::graphdir,
    http_compression                     => $puppet::params::agent::http_compression,
    waitforcert                          => $puppet::params::agent::waitforcert,
    ordering                             => $puppet::params::agent::ordering,
    archive_files                        => $puppet::params::agent::archive_files,
    archive_file_server                  => $puppet::params::agent::archive_file_server,
    tagmap                               => $puppet::params::agent::tagmap,
    sendmail                             => $puppet::params::agent::sendmail,
    reportfrom                           => $puppet::params::agent::reportfrom,
    smtpserver                           => $puppet::params::agent::smtpserver,
    smtpport                             => $puppet::params::agent::smtpport,
    smtphelo                             => $puppet::params::agent::smtphelo,
    dblocation                           => $puppet::params::agent::dblocation,
    dbadapter                            => $puppet::params::agent::dbadapter,
    dbmigrate                            => $puppet::params::agent::dbmigrate,
    dbname                               => $puppet::params::agent::dbname,
    dbserver                             => $puppet::params::agent::dbserver,
    dbport                               => $puppet::params::agent::dbport,
    dbuser                               => $puppet::params::agent::dbuser,
    dbpassword                           => $puppet::params::agent::dbpassword,
    dbconnections                        => $puppet::params::agent::dbconnections,
    dbsocket                             => $puppet::params::agent::dbsocket,
    railslog                             => $puppet::params::agent::railslog,
    rails_loglevel                       => $puppet::params::agent::rails_loglevel,
    couchdb_url                          => $puppet::params::agent::couchdb_url,
    tags                                 => $puppet::params::agent::tags,
    evaltrace                            => $puppet::params::agent::evaltrace,
    summarize                            => $puppet::params::agent::summarize,
    ldapssl                              => $puppet::params::agent::ldapssl,
    ldaptls                              => $puppet::params::agent::ldaptls,
    ldapserver                           => $puppet::params::agent::ldapserver,
    ldapport                             => $puppet::params::agent::ldapport,
    ldapstring                           => $puppet::params::agent::ldapstring,
    ldapclassattrs                       => $puppet::params::agent::ldapclassattrs,
    ldapstackedattrs                     => $puppet::params::agent::ldapstackedattrs,
    ldapattrs                            => $puppet::params::agent::ldapattrs,
    ldapparentattr                       => $puppet::params::agent::ldapparentattr,
    ldapuser                             => $puppet::params::agent::ldapuser,
    ldappassword                         => $puppet::params::agent::ldappassword,
    ldapbase                             => $puppet::params::agent::ldapbase,
    templatedir                          => $puppet::params::agent::templatedir,
    allow_variables_with_dashes          => $puppet::params::agent::allow_variables_with_dashes,
    parser                               => $puppet::params::agent::parser,
    max_errors                           => $puppet::params::agent::max_errors,
    max_warnings                         => $puppet::params::agent::max_warnings,
    max_deprecations                     => $puppet::params::agent::max_deprecations,
    document_all                         => $puppet::params::agent::document_all,
  }
) inherits ::puppet::params::agent {
  file { '/tmp/agentini.txt':
    ensure  => present,
    content => template('puppet/ini_generator.erb'),
  }
}
